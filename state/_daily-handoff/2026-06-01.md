# Daily handoff: 2026-06-01

> Outgoing from: Claude (Opus 4.8 1M), recovery session after the 2026-05-31 crash
> For: next Brand Strategy & Sonagi OS session
> Branch: claude/live-baseline-2026-05-14
> Launch gate: n/a (internal OS infra, no site/content change)

## What this session shipped

### Infra (committed)
- `8b3f69f` feat(OS): inject kernel at token zero + make hooks Mac-portable. New `scripts/emit_kernel.py`, wired into the SessionStart hook, so a fresh session is injected ~3k tokens of kernel (charter + agent roster + both maps) by mechanism, not by trusting the agent to read `core/`. This closes the central flaw: the cache was regenerated but nothing loaded it. All hooks standardized on `python3` (works on Windows + macOS); `.githooks/pre-commit` too.
- `ae5dfdc` chore(OS): gitignore working content + skill junctions; de-litter repo root. Deleted 25 zero-byte shell-redirect junk files (incl. the empty `.prod-card`, a false alarm). Ignored OneDrive-only working content + the external skill junctions. 7 teaching images moved to `brand-strategy/brand-identity/prompt-example-images/`.
- `e0d6928` fix(OS): map generator respects `.gitignore`. `gen_project_map` skips ignored folders via `git check-ignore`, so the token-zero map no longer injects `node_modules`/`MCP connectors`/`media` noise. Hidden count reported (no silent truncation).

### Process / non-commit
- Detached the stale OneDrive `.git` (5 commits behind, zero unpushed; was a corruption risk). OneDrive is now binaries-only per Charter D2; git+Notion stay the portable cloud source of truth.
- Mac migration: authored `OneDrive/laptop-migration-2026-05-29/global-settings-mac.json` (python3 + $HOME paths) and corrected `NEW-MAC-SETUP.md` (added Python install, the `sonagi-agents` clone into `~/.claude/agents`, the `~/.agents` restore the symlink script needs). Marouane's Mac setup is IN PROGRESS (reached step 7, symlink rebuild).
- Karpathy-style review of v4 + Asma's 4 target diagrams. Verdict: the foundation is now actually laid; divergence from the diagrams is mostly deliberate (fleet machinery deferred for a 2-person team). The ONE unintentional gap is durable crash-resume, and the 2026-05-31 crash proved it.
- Generated 3 "intentional" architecture diagrams (what we chose vs what we skipped, for our size) in Sonagi branding: `images/2026-06-01/os-diagrams/v3-01-agents.png`, `v3-02-memory.png`, `v3-03-cost.png`. For LinkedIn.

## Next session queue (priority order)
- **A. Durable session state / crash-resume. NEW #1.** The crash exposed it; the diagrams' "durable state, resumes exactly" is the unbuilt gap. Empirically the highest-value next build (ahead of the sales agent). Scope: checkpoint enough of an interactive session that a crash resumes without re-reading a transcript by hand.
- **B. Remaining repo litter (37 untracked).** Some is CODE that should be TRACKED so it travels to the Mac: `netlify/edge-functions/`, `scripts/*.py`, `scripts/engagement-brief/`. Some is junk to delete: `website/*.crdownload` (partial downloads), `website/files.zip`, `website/unzipped/`. Some are OneDrive-only notes: `website/*.txt`. Triage and either track or ignore.
- **C. Sales agent** (prior queue A): distill the Downloads sales books into ONE `sales-playbook` skill, then the agent. Never load raw books at runtime.
- **D. Retire the old no-mirror rule** on the AI Ops page + decision-log it (Charter §2 already reconciled).
- **E. Editorial Lead + Reference Backend agents** (ground in EDITORIAL-BIBLE first, do not invent).
- **F. Demote the 6 roster roles to real skills** (write SKILL.md, register in Manifest).
- **G. Migrate the Rain/FastMoss orphan handover** into the Handoffs DB as a Social Content row.
- **H. Gate the Stop auto-push** of the agents repo (it publishes to GitHub unattended on every session end, in tension with the human-gate-on-publish rule). Either gate it or remove the contradiction from the Charter.

## Standing decisions landed this session
- Kernel injection at token zero is the cold-start mechanism (commit 8b3f69f).
- OneDrive = heavy binaries only. Code -> git+GitHub, strategy -> Notion. Never put a live git repo in OneDrive (it corrupts).
- All hooks/scripts standardized on `python3` for cross-OS (Windows + Mac).
- Diagrams must present the INTENTIONAL selection for our size (chose vs deliberately skipped), not the generic enterprise menu.

## Branch / preview / production state at close
- Branch `claude/live-baseline-2026-05-14`, last commit `e0d6928`. Local only, not pushed (no auto-push on this repo; pushing is Asma's call).
- Machine brain `asmabak/sonagi-agents`: clean, synced to origin/main.
- No preview/PR (internal OS infra). Production sonagibeauty unaffected.

---

# Daily handoff: 2026-06-01 (SESSION 2, queue execution)

> Outgoing from: Claude (Opus 4.8 1M). For: next Brand Strategy & Sonagi OS session.
> Branch: claude/live-baseline-2026-05-14. Launch gate: n/a (internal OS infra).
> This session executed the prior handover's queue A-H, plus bonus work Asma asked for mid-session.

## What this session shipped (11 commits, all auto-pushed)
### Queue item A: durable crash-resume (DONE)
- `5355bf8` feat(OS): crash-resume checkpoint. `scripts/checkpoint_session.py` (Stop + UserPromptSubmit hooks) snapshots the live session to `state/_checkpoint/latest.{json,md}`; `scripts/resume_check.py` (SessionStart) surfaces a `<sonagi-resume>` banner if a checkpoint survived (= prior session did not exit cleanly); `scripts/clear_checkpoint.py` (SessionEnd + wrapup step 9) deletes it on clean exit. Crash detection = absence of a clean SessionEnd. `evals/crash_resume_eval.py` PASS. Wired into `.claude/settings.json`. `state/_checkpoint/` gitignored.
- `f154d58` fix(OS): hardened per the engineering-review (SONAGI_CKPT_DIR env override for true eval isolation, atomic write, last-prompt precedence, age hint on the banner).
### Queue item B: untracked-file triage (DONE)
- `719fdfe` + `e85d5c8`: tracked real code (netlify/edge-functions/geo.js, deno.lock, docs/release-protocol.md, scripts/*.py, scripts/engagement-brief 3 lib files); gitignored OneDrive working content; DELETED (verified lossless) website/files.zip, 3 *.crdownload partials, the crash dump. Repo is now clean (0 untracked). `__pycache__/` ignored.
### Queue item D (DONE): retired the no-mirror principle on the Notion AI Ops page (decision-log row + banner) + `core/decisions.md`. Superseded by the two-brains model.
### Queue item G (DONE): migrated the Rain/FastMoss orphan handover into the Session Handoffs DB as a Social Content row (https://www.notion.so/37278492123f81c382bbf0239f25d4f4); banner added to the original.
### Queue item H (DONE): verified the backup repo is PUBLIC (intentional, for Netlify publishing, confirmed by Asma). Built `core/decisions.md` (a standing decision log) + `59c483d`/`bf042a7`: kernel carries a POINTER to it (read on demand, not preloaded). Corrected push_repo.py docstring.
### Bonus (Asma asked mid-session)
- `71f9eda` feat(skill): `skills/engineering-review/SKILL.md` (Karpathy lens + Gate 1 YAGNI scope filter). Run in a fresh context, never self-review.
- `8e4f550` fix(eval): em-dash gate scoped to CONTENT files only (.md/.mdx/.txt/.html), never code. Per Asma: the em-dash ban is content-writing only.
- `7abfa6a` + `30af9a7` docs: 8 coding principles documented in `core/learnings.md` (incl. context-budget awareness).
- 4 LinkedIn diagrams in `images/2026-06-01/os-diagrams/`: skills-vs-agents, review-is-a-skill-scope-gate, engineering-principles, context-budget (locked Sonagi fine-liner style, professional/technical register).
- Memories saved: surface-coding-principles, decision-log-and-background.

## Next session queue (priority order): C, E, F deferred here, with reasons
- **C. sales-playbook SKILL only; DEFER the sales agent.** Distill the Downloads sales books into ONE `skills/sales-playbook/SKILL.md` (never load raw books at runtime). The review's verdict: the skill passes Gate 1, the AGENT does not (no active sales motion pre-launch). Build the agent only when a real recurring sales job exists.
- **E. Editorial-bible SKILL likely; DEFER the 2 agents until justified.** Editorial Lead + Reference Backend agents were flagged as proliferation risk. Ground anything in `sonagi-reference/EDITORIAL-BIBLE.md` first, do NOT invent. For each proposed agent, answer: "what look-decide-act loop does this run that an existing agent + a skill cannot?" before writing it.
- **F. Demote 6 roster roles to skills (analysis done, execution deferred).** The 6 Notion roster roles that are really skills (per the agent-vs-skill litmus): Brand Voice Guardian, French Localizer, K-beauty Expert, Beauty Image Director, Creative Director (→ Brand Compliance Checker), SEO/AEO/GEO Strategist. The genuine agents that STAY: Chief of Staff, Max, Rain, Social Media Strategist, Release Manager, QA, Medical Fact-Checker, Web Engineer. Next: confirm the exact 6 with Asma, write each `SKILL.md`, register in the Manifest, reclassify on the Notion AI Ops page. This is high-grounding work, do it fresh.

## Open verifications / follow-ups
- **CRASH-RESUME ASSUMPTION (unproven):** confirm Claude Code's SessionEnd hook fires on a clean /clear+quit but NOT on a hard kill, on BOTH Windows and macOS. Do one manual test per OS (clean-quit → checkpoint gone; kill -9 → checkpoint survives). Log result in learnings. If SessionEnd is flaky, the resume banner could false-positive.
- **Charter wording (Notion):** reword the Sonagi OS Charter's "private-backup push is not a publish" line to match reality (the repo is public by design; the guard is never-commit-secrets). Mirror to core/charter.md after.
- **~/.claude/commands/ is NOT git-tracked.** The /wrapup edit (step 9 checkpoint-clear) lives only on this machine. Consider version-controlling the global commands for the Mac move.
- **Manifest registration:** register `engineering-review` skill + note the crash-resume scripts (done in this wrapup if reached).

## Standing decisions landed this session (see core/decisions.md)
- Repo is PUBLIC on purpose (Netlify). Never commit secrets. Do not flip to private without checking Netlify/Pages.
- Em-dash ban is content-only, not code.
- Bias to skills over agents; a review standard is a skill (not a persona agent).
- No-mirror principle retired (two-brains model; mirrors that follow a master are allowed).
- Decision log is stored + pointed-to (read on demand), not fed to every session.
- Context-budget awareness: stop before quality drops, flag the limit, clean handoff beats degraded push.

## Branch / preview / production state at close
- Branch `claude/live-baseline-2026-05-14`, last commit `87bcf54`. 11 commits this session, pushed to origin (public backup).
- No preview/PR (internal OS infra). Production sonagibeauty unaffected.
