# Daily handoff: 2026-06-02

> Outgoing from: Claude (Opus 4.8 1M). For: next Brand Strategy & Sonagi OS session.
> Branch: claude/live-baseline-2026-05-14. Launch gate: n/a (internal OS infra, no site/content change).
> This session picked up the 2026-06-01 16:35 handover (queue: command distribution, then sales-playbook).

## What this session shipped

### Command / definition portability (the top-priority queue item)
- Reframed the problem with Asma: it is not "sync 2 command files," it is "full structure on any device, never blocked." Wrote the first-principles analysis `OS/device-portability-architecture.md` and ran a fresh-context Karpathy/Gate-1 review that CUT the originally-proposed sync script as an over-build for 2 monthly-changing files.
- Decision (logged in core/decisions.md): canonical master = the sonagi-beauty repo under `.claude/`; all other copies are generated mirrors. v1 stays MANUAL (no sync_defs.py yet); build the script only when agents + skills join (~12+ files).
- `ef87be0` docs(OS): device-portability architecture + R2 command propagation (v1 shipped).
- v1 R2 executed: placed lets-start-sonagi + wrapup on BOTH repos' DEFAULT branches so a phone session sees them. sonagi-beauty default = claude/extract-marketing-skills-z03eL; sonagi-reference default = main. Verified present via the GitHub API. No default-branch change, so zero Netlify risk.

### wrapup divergence fixed (was follow-up #1)
- Diagnosed the two wrapup.md copies as DRIFT, not legitimate per-repo difference: sonagi-beauty's (182 lines) was the newer superset (has the Build-Chart/Manifest step 6b and a crash-checkpoint-clear in step 9); sonagi-reference's (161 lines) was an older copy.
- Unified on the sonagi-beauty master; step 9 now degrades gracefully (no-op) when scripts/clear_checkpoint.py is absent, so the same file is correct in the reference repo. Mirrored to all 5 locations: ~/.claude/commands, sonagi-beauty working + default branches, sonagi-reference phase-0/init + main. lets-start-sonagi was already byte-identical everywhere.
- `41f8b9b` chore(commands): unify wrapup master (graceful step 9) + log 3 decisions.

### Hook bug fixed + queue item H resolved
- The "Stop hook script missing" error Asma saw was a Windows backslash path (`C:\Users\...\sync_agents.py`) that the hook shell mangled into a garbage relative path. Fixed both the SessionStart `pull` and Stop `push` hooks in ~/.claude/settings.json to forward slashes. Verified the path resolves.
- Asma chose to KEEP the agents-repo auto-push on (a backup push is saving, not a gated publish). Queue item H resolved as "leave it on."

### Crash-resume question answered (no code change)
- Ran evals/crash_resume_eval.py: PASS. Confirmed via claude-code-guide that SessionEnd cannot fire on a hard kill (SIGKILL/power loss), so a real crash leaves the checkpoint intact and the next session shows the resume banner. The gray-zone signals (Ctrl+C, terminal close, sleep, internal exception) are undocumented and the `reason` field cannot distinguish a crash from a clean exit; worst case there is just no banner, never a wrong one that loses work.
- Honest summary given to Asma: the checkpoint protects ORIENTATION (where you were), not unsaved files. Files are protected only by commit + push.

### Decisions + memory
- core/decisions.md: 4 new entries (portability canonical home + v1 R2; wrapup unified; agents auto-push stays on; em-dash ban clarified to reader-facing content only).
- Memory updated: feedback_no_em_dashes refined to content-only per Asma 2026-06-02.

## Standing rules / clarifications landed this session
- **Em-dash ban is for reader-facing CONTENT only** (articles, copy, captions, emails, social, newsletter). Code, config, command/skill/agent files, and internal OS docs (OS/, core/, state/) CAN have em dashes. Asma verbatim 2026-06-02: "I do not care about em-dashes in the code but just in the content that is written for me. The code can have em dashes." The commit gate already skips `.claude/` and code; it still scans OS/ and core/ as a conservative net, which is acceptable.
- **Agents-repo auto-push stays ON** (queue H). To disable later, remove the Stop `push` hook from ~/.claude/settings.json.

## Next session queue (priority order): all sonagi-beauty, none clash with Reference article work
- **A. sales-playbook SKILL (the prior top queue item, do FRESH).** Distill the 5 Downloads sales books into ONE skills/sales-playbook/SKILL.md, never load raw books at runtime. Corpus already identified: `_OceanofPDF.com_00M_Offers_Made_Easy_-_Ben_Preston.pdf`, `100m_leads_-_Alex_Hormozi.pdf`, `100M_Offers_-_The_Lost_Chapter_-_Alex_Hormozi.pdf`, `Cashvertising_-_Drew_Eric_Whitman.pdf`, `INFLUENCE_-_Robert_B_Cialdini.pdf` (all in C:/Users/marou/Downloads). DEFER the sales agent (no active sales motion pre-launch; skill passes Gate 1, agent does not). Register the skill in the Manifest. This is high-grounding work: read the books, do not paraphrase from memory.
- **B. Default-branch cleanup (R1), BLOCKED on Asma.** Both repos' defaults are stray/near-empty (sonagi-beauty default = claude/extract-marketing-skills-z03eL; sonagi-reference default = main, just an Initial commit). Repointing them is the proper fix but can change what Netlify deploys. NEED FROM ASMA: the Netlify Production branch (dashboard -> Site config -> Build & deploy -> Continuous deployment -> Production branch) for the sonagi-beauty site (and the reference site if any). Only repoint after that is confirmed.
- **C. Demote 6 roster roles to skills (analysis done, execution deferred, high-grounding).** Brand Voice Guardian, French Localizer, K-beauty Expert, Beauty Image Director, Creative Director (to Brand Compliance Checker), SEO/AEO/GEO Strategist. Genuine agents that STAY: Chief of Staff, Max, Rain, Social Media Strategist, Release Manager, QA, Medical Fact-Checker, Web Engineer. Confirm the exact 6 with Asma, write each SKILL.md, register in the Manifest, reclassify on the Notion AI Ops page (35e78492-123f-81ab-b096-fabedb814a0b). Do FRESH.
- **D. editorial-bible SKILL likely; DEFER the 2 agents (E in old queue).** Ground in sonagi-reference/EDITORIAL-BIBLE.md first, do NOT invent.

## Open verifications / follow-ups
- CRASH-RESUME manual test (still unproven on the real lifecycle): one test per OS. Clean /clear+quit -> checkpoint gone; hard kill -> checkpoint survives + banner next session. Log in learnings. SIGKILL case is logically certain; the gray-zone signals need the empirical test.
- wrapup mirror-refresh is still MANUAL. When agents + skills join the portability machinery, wire a refresh step into /wrapup (or build sync_defs.py).
- ~/.claude/commands/ and ~/.claude/settings.json are NOT git-tracked; the hook path fix lives only on this machine. Version-control the global commands + settings before the Mac move.
- Charter wording (Notion): reword the "private-backup push is not a publish" line (repo is public by design; the guard is never-commit-secrets). Mirror to core/charter.md after.

## Branch / preview / production state at session close
- Branch claude/live-baseline-2026-05-14, last commit 41f8b9b. 2 commits on this branch this session, pushed. Also pushed: sonagi-beauty default branch (95c012c), sonagi-reference phase-0/init (620cbb8) and main (d14765c), all command-mirror commits, all out and clean (no stray worktrees).
- Only uncommitted items: core/PROJECT_MAP.md (auto-generated each session, not session work) and a stray OS/sonagi ref cras.txt (pre-existing crash dump, not this session's).
- No preview/PR (internal OS infra). Production sonagibeauty unaffected.
- NOTE: Asma started Sonagi Reference article work in parallel at session end. This session did NOT touch the reference repo after that point. Do not assume the reference repo state from here; re-read it fresh.

---

# Daily handoff: 2026-06-02 (SESSION continued, LIVE SITE OUTAGE, unresolved)

> Outgoing from: Claude (Opus 4.8 1M). Stream: Site & E-commerce (incident).
> Branch: claude/live-baseline-2026-05-14. STATUS: sonagibeauty.com is DOWN (HTTP 503).

## INCIDENT: sonagibeauty.com returns 503 usage_exceeded (NOT resolved)

**Symptom:** every URL (apex + www, even static HTML) returns
`HTTP 503 {"error":"usage_exceeded","message":"Usage exceeded"}` from Netlify.
Confirmed live at session close.

**Diagnosis (high confidence on cause, NOT yet confirmed which quota):**
- Netlify site id: 545b559c-e99f-4a48-9b39-d03fffc2529e. Free tier, no payment method (Asma will NOT pay more, hard constraint).
- `usage_exceeded` = a Netlify plan usage limit was hit. Two candidate causes:
  1. EDGE FUNCTION quota: `netlify/edge-functions/geo.js` ran on path `/*` (EVERY request) and rewrote the full HTML of every response. A recently-added /* edge function exhausting the free edge-function allowance fits a site-wide 503.
  2. BANDWIDTH: the site was 127 MB, 118 MB of it video (93%), incl. a 33.7 MB sonagi-rain-web.mp4 autoplaying on the homepage.
- Could be either or both. Only the Netlify dashboard Usage page disambiguates, and this session had NO Netlify access (no token, no CLI auth; the saved login is in Edge but Edge was running so the profile was locked for Playwright).

**Fixes ALREADY committed + pushed to claude/live-baseline-2026-05-14 (likely prod branch, unconfirmed):**
- `2733803` perf(site): removed ALL 9 homepage videos, replaced with their existing poster photos (the wall is now a static photo grid placeholder). Kept ONE video, "eat your skincare", moved to the about page, re-encoded 6.8 MB -> 0.32 MB. Cuts homepage video bandwidth ~90%. Validated: 0 videos on homepage, 1 on about, CSS already styles img, 0 em dashes. NOT yet visually previewed (Asma gate) and NOT yet confirmed live.
- `fe65730` fix(site): disabled the /* geo edge function (renamed geo.js -> geo.js.disabled) so it stops consuming the edge-function quota. Reversible (rename back). Language autodetect falls back to the client-side detector.

**What did NOT work:** after pushing fe65730, polled sonagibeauty.com for 6 min, stayed 503. Means EITHER (a) claude/live-baseline-2026-05-14 is NOT the Netlify production branch (so the push triggered no deploy), OR (b) the block is account/site-level usage (a redeploy cannot lift it; only the monthly reset or a plan change can).

## NEXT SESSION: resolve the outage FIRST. Exact steps.

1. **Get Netlify dashboard access.** Either Asma reads it (she is logged in on Edge) or close Edge so Playwright can use the saved login (playwright 1.60 installed; browsers at AppData/Local/ms-playwright; `node require('playwright')` did NOT resolve from cwd, fix the require path or use `npx`). Need THREE facts:
   a. Usage page: WHICH resource is over (Bandwidth / Edge Functions / Functions / Build minutes) + the RESET DATE.
   b. Deploys tab: did a deploy run after commit fe65730 (07:18 UTC)? status?
   c. Site config -> Build & deploy -> PRODUCTION BRANCH (the recurring unknown all session).
2. **If production branch != claude/live-baseline-2026-05-14:** push the two fix commits (2733803 + fe65730) to the real production branch so the auto-deploy serves the static, edge-function-free site.
3. **If the over-limit resource is Edge Functions:** the geo removal is the fix once deployed to the prod branch. Confirm site returns 200 (curl).
4. **If it is Bandwidth (hard account cap):** no redeploy fixes the current cycle. Free options only (Asma will not pay): wait for the reset (use the date from 1a), or move the host (Asma said "no need for dns change, I already sorted that out to automate", clarify what that automation is). Cloudflare Pages = free unlimited bandwidth if a move is wanted.
5. After restore: visually preview the homepage (hero photo slides + static photo wall) + about page (eat-skincare video) before declaring done. Then update docs/release-protocol.md (it is STALE: it says manual deploy; Asma confirmed deploy is now automated via Netlify's native GitHub integration). Record the production branch there so this is never re-discovered.

## Standing decisions landed this session (committed)
- `a4e8ef4` AUTONOMY ENVELOPE (Asma 2026-06-02, "Full + record it"): run research -> draft -> diagrams -> build -> push -> preview autonomously. Hard stops ONLY: (1) any image of a real person, (2) charter publish/spend/delete gate + Reference launch gate. Logged in core/decisions.md; memory feedback_autonomy_envelope. MIRROR TO NOTION decisions log (pending).
- Em-dash ban clarified to reader-facing CONTENT only (code/internal docs exempt); the commit gate still scans OS/core as a conservative net (future tweak: narrow to content paths).

## Branch / production state at close
- Branch claude/live-baseline-2026-05-14, last commit a4e8ef4. All session commits pushed.
- PRODUCTION: sonagibeauty.com = HTTP 503 (DOWN). This is the #1 item.
- Uncommitted (NOT session work): auto-generated core/ caches (PROJECT_MAP, agent_chart, skills_index), a dropped OS/Nano_Banana_Pro_Prompts_.pdf, and OS/sonagi ref cras.txt.
- Brand Strategy & Sonagi OS has its OWN separate Open handover from earlier today (portability v1 done; queue: sales-playbook skill, demote 6 roster roles, editorial-bible skill).

---

# Social Content (Rain films) — PM session

> Outgoing from: Claude (Opus 4.8 1M). For: next Social Content session.
> Branch: claude/live-baseline-2026-05-14. Launch gate: n/a (creative dev, nothing posted, nothing deployed).
> Stream: Social Content. Picked up the Rain Videos + FastMoss handover, worked the Rain films half only.

## What this session produced (creative assets — all in `media/visuals sonagi reels under the rain/`)
- Expanded the Rain "writers' room": added director souls Céline Sciamma, David Lean (Brief Encounter), Sofia Coppola, Richard Linklater, Luca Guadagnino, and Emerald Fennell/Jacob Elordi (Wuthering Heights, the windswept-moor intensity) to the existing 3 (WKW / Ephron-Meyers / Rohmer-Truffaut). NOT yet saved to the canonical Notion Rain page (offered, Asma didn't confirm).
- End-scene reference library extended A–F (existing) to A–V: 22 variants, mixed/diverse couples, recognizable cities (Paris/NY/London/Venice/Seoul) + unknown countryside (moor/wheat field/sea cliff/misty hills). NOTE: variants A–V still carry the OLD "cinematic film still / fine film grain" tail and must be reissued in the phone-real register (see rules). NOT saved to Notion.
- Story / opening-scenario Library: 14 meet-cute openings with circumstance + stakes (the worst day, runaway bride, phone breakup, split grocery bag, the busker she shelters, missed train, the moor, bus splash, lost traveler, last goodbye, child matchmaker, beach umbrella, one awning, after the shift). In chat only — NOT saved to a file. SHOULD be saved.
- `media/visuals sonagi reels under the rain/FILM-01-the-worst-day-SCRIPT.md` — the only saved script file. Full shot script for Film #1 (Seoul neon, Latina woman + Korean man) + a Sora one-shot master prompt appended.
- Stills Asma generated in ChatGPT this session: Variant G, Variant H, Variant I (the Seoul end signature, used as Film 01 beat 3), and `STILL 1 — The worst day, the fall.png` (phone-real, approved).

## Standing rules saved to memory this session (same severity as em-dash ban)
1. `feedback_never_sexualize_rain_films.md` — the rain films and all Sonagi romantic visuals must NEVER sexualize, even in stills. Emotion reads through faces/eyes/hands/gaze only. Ban bare/cropped/clinging/translucent/wet-cling. Fully clothed.
2. `feedback_rain_films_phone_real.md` — BRAND IDENTITY: the films must look phone-real / "I witnessed this and caught it on my phone", NEVER glossy cinematic. Precisely prompt phone camera (~26mm), available light only, lens rain, handheld, flat phone HDR, sensor grain; drop cinematic/film-grain/grade/genre language. Medium witness framing beats extreme close-up. The viewer must imagine themselves in the scene.

## Failures this session — READ THIS, do not repeat (cost: ~80 of 1131 Higgsfield credits on 2 UNUSABLE clips)
- Clip 1 (72 credits, seedance_2_0 1080p): animated the Variant I END still with MASTER MOTION and mislabeled it "the first movie". It is only the closing beat, not a story. Right technique, wrong scope.
- Clip 2 (8 credits, veo3_1_lite, single-still-forward from STILL 1): FAILED — the man never appeared (only a disembodied hand + umbrella), she never stood, no eyes-meet. Root cause: a video model CANNOT invent a second person or a big pose change out of a still of one person. And the prompt broke the Seedance guide (≈120 words, 4 actions, multiple camera moves in one clip).
- The two reliable methods, per the guide + what actually worked: (a) animate a still where everyone is ALREADY present, with ONE small motion (the 72-credit end clip held perfectly for this reason); (b) START frame + END frame interpolation between two real stills (seedance supports start_image + end_image), so the model fills motion instead of inventing.

## Next session queue (priority order) — ASMA'S EXPLICIT INSTRUCTION
A. **BECOME THE ABSOLUTE EXPERT ON HIGGSFIELD / SEEDANCE PROMPTING BEFORE WRITING A SINGLE PROMPT.** Read, in full, BEFORE any generation:
   - `C:\Users\marou\higgsfield-seedance2-skills\SEEDANCE-PROMPT-GUIDE.md` (the golden rule: 60–100 words, ONE subject / ONE action / ONE camera per segment, time-segments for long clips, affirmative not negative, @reference first-frame/last-frame system, lighting is highest leverage).
   - `C:\Users\marou\higgsfield-seedance2-skills\SCRIPT-WRITING-TEMPLATE.md` and the SCRIPT-01..05 examples.
   - The 16 seedance skills at `~/.claude/skills/seedance/` (esp. 01-cinematic, 11-social-hook, 12-brand-story, 16-influencer-mirror-selfie).
   - branded-ai-design skill. Asma's words: "become the absolute expert on writing prompts for higgsfield, it needs to read my skills." She was (rightly) angry that prompts were written without consulting the skills library.
B. Produce Film #1 ("the worst day") the RELIABLE way: confirm with Asma, then either (i) Asma makes a STILL 2 (umbrella already open + his hand/arm already in frame, she looking up) and do two small same-framing interpolations STILL1→STILL2 then STILL2→VariantI; or (ii) one start→end interpolation STILL1→VariantI. Cheap model first (seedance_1_5 ~10cr or seedance_2_0 720p =36cr). 720p is the ceiling (1080p is wasteful and reads less real). PREVIEW COST with get_cost and get Asma's GO before every paid generation — she is highly credit-sensitive.
C. Persist the creative work to the canonical Notion Rain page (37078492-123f-81a7-be22-ecc7f1150571): the expanded director roster, end-scene variants G–V reissued in the PHONE-REAL register, and the 14-opening Story Library. Backup, not a publish.
D. (Other half of the original handover, untouched) FastMoss MCP: install + connect on Windows, confirm endpoint paths. See Notion 37078492-123f-8181-a87b-ecf6bc806d3c.

## Hard cost rules learned
- get_cost preflight + explicit Asma GO before EVERY paid generation.
- 720p ceiling. 1080p banned for these (wasteful + too glossy).
- Never ask a model to invent a person or a big pose change from a single still. Use start+end interpolation or a still where everyone is present.

## State at session close
- Branch claude/live-baseline-2026-05-14, no new repo commits (assets are untracked working files in media/; rules in ~/.claude memory).
- Higgsfield Plus balance ~1051 credits (was 1131, spent ~80).
- Nothing posted, nothing deployed. Site 503 incident still open from earlier sessions (separate stream).
